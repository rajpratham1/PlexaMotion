# Core Frameworks & Server
Flask>=3.0.0
gunicorn
Werkzeug>=3.0.0
Jinja2>=3.1.2
MarkupSafe>=2.1.3
itsdangerous>=2.1.2
click>=8.1.3
blinker>=1.6.2

# Computer Vision (The CRITICAL FIX: Pin NumPy to < 2.0.0)
numpy<2.0.0             # MUST be < 2.0.0 to satisfy OpenCV 4.x ABI
opencv-contrib-python-headless==4.9.0.80 
# (Note: mediapipe is implicitly handled by the try-except in air_writer.py)

# Data Processing / ML Libraries (Keep generic, but rely on pinned NumPy)
protobuf>=4.25.1
pillow>=10.2.0

# PDF & Image Handling
PyMuPDF>=1.24.0

# Other Dependencies
matplotlib>=3.8.0
