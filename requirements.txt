# Core Frameworks & Server
Flask>=3.0.0
gunicorn
Werkzeug>=3.0.0
Jinja2>=3.1.2
MarkupSafe>=2.1.3
itsdangerous>=2.1.2
click>=8.1.3
blinker>=1.6.2

# Computer Vision (Keep only the stable, main library)
opencv-contrib-python-headless==4.9.0.80 

# Data Processing / ML Libraries (Remove problematic JAX/NumPy/Protobuf dependencies)
# We rely on the system's default NumPy/Protobuf if needed by OpenCV, but we don't pin them.

# PDF & Image Handling
PyMuPDF>=1.24.0
pillow>=10.2.0

# General dependencies
matplotlib>=3.8.0
